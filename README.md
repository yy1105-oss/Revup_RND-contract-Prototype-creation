
  # Prototype creation

  This is a code bundle for Prototype creation. The original project is available at https://www.figma.com/design/zU0cS07RvngTsoOfSd71Mw/Prototype-creation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  